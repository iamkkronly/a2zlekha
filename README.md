# Typography

Use `A2Z Photo Hand` for the supplied handwritten `A–Z`, `a–z`, and `0–9` glyphs (`.woff2` for web, `.ttf` for apps).

For every other text style, use rich, readable typography with intentional font pairing, hierarchy, scale, weight, spacing, and line-height.
